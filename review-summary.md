# 项目代码评审摘要

> 评审方式：逐项目阅读本地源代码（非仅凭简历/记忆），从**技术架构、功能完整性、技术难点、工程落地**四个维度评估。
> 本文档同时说明：哪些能力写进了网站、哪些因"源码不支持"未写入——确保作品集经得起面试官追问。

---

## 一、总览

| 项目 | 源码位置 | 评审结论 | 站内呈现 |
|------|---------|---------|---------|
| 果蔬成熟度智能检测系统 | `D:\课程项目\深度学习\…\水果蔬菜识别系统` | ✅ 完整可运行，工程化程度高 | P1 核心项目 |
| BOSS直聘自动化投递助手 | `D:\求职与实习\boss-automation` | ✅ 三层架构完整，风控有源码实证 | P2（原名恢复） |
| 中文语音情感识别系统 | `D:\毕业设计\speech-emotion-recognition` | ✅ 多模态融合，全栈完整 | P3（毕业设计） |
| 网页记账本系统 | `D:\workbuddy`（v1→v3.1 迭代记录完整） | ✅ 业务建模清晰，已上线 | P4（已上线） |
| 副屏硬件监控（SensorPanel） | **全盘未找到源码** | ❌ 无法核验 | 未收录 |
| RAG / 低代码 / 工作流独立项目 | **无独立仓库源码** | — | 写入技能栈与实习经历，未虚构为项目 |

---

## 二、逐项目评审

### 1. 果蔬成熟度智能检测系统（P1）

**评审范围**：`MainProgram.py`（628 行）、`UIProgram/`（UiMain.py 29KB）、`Config.py`、`detect_tools.py`、`train.py`、`yolov8n.pt`、`datasets/`

**技术架构**：Ultralytics YOLOv8 目标检测 + PyTorch，OpenCV 图像处理，PyQt5 桌面 GUI。

**功能完整性（源码实证）**：
- 四种推理模式：单张图片 / 视频文件 / 摄像头实时 / 目录批量（`open_img` / `vedio_show` / `camera_show` / `detact_batch_imgs`）
- 结果可视化：检测框绘制、目标下拉筛选（`combox_change`）、置信度与坐标表格（`tabel_info_show`）、置信度/IOU 阈值可调
- 视频导出：QThread 后台线程逐帧推理（`btn2Thread`），带进度条与取消机制，不阻塞 UI
- 工程细节：CUDA/CPU 自适应（`torch.cuda.is_available()`）、模型预加载推理、HighDPI 屏幕适配

**量化指标**（来自项目资料/简历）：自建标注数据集 8000+ 张；综合识别准确率 93%。

**⚠️ 重要核验说明（诚实红线）**：
- 原始需求将本项目命名为"果蔬成熟度智能检测&语音问答**一体化**系统"，并要求补充"语音识别与 NLP 问答引擎、副屏显示双屏交互、智能助手实时对话"。
- **源码实测：本项目为纯视觉检测系统，不含任何语音、NLP 问答或副屏相关代码。** 为守住"不虚构"底线，站点按真实功能撰写。
- 「语音问答/大模型对话」能力由**中文语音情感识别系统**（P3，独立项目）真实承载；副屏项目因源码缺失未收录。
- 如你确实有"果蔬 + 语音/副屏"的融合版本代码，请提供路径，我可以合并重写本项目描述。

### 2. BOSS直聘自动化投递助手（P2 · 原名恢复）

**评审范围**：`BOSS助手.user.js`（34KB 用户脚本）、`app.py`（FastAPI 后端）、`dashboard.html`（看板）

**技术架构**：浏览器脚本层（Tampermonkey）+ FastAPI 服务层 + SQLite 持久层 + SSE 实时看板，LLM API（DeepSeek）智能回复。

**功能完整性（源码实证）**：
- 页面交互层：滚动加载职位卡片、焦点/键入/点击事件模拟（`dispatchEvent`）、多选择器兜底适配不同页面结构、简历 PNG 上传
- 智能对话：接入 DeepSeek API 生成招呼语与多轮回复，失败重试（retry ×3）
- 数据层：SQLAlchemy ORM 定义 `ApplyLog`（投递日志）/`DailyStat`（每日统计）双表，PDF→PNG 简历转换兜底
- 实时看板：`/api/sse` Server-Sent Events 推送，看板实时刷新今日/累计数据、投递明细与日志流

**四层风控策略（源码实证）**：
1. **频率控制**：全链路分级 `sleep()` 等待，操作节奏受限速约束
2. **随机化间隔**：操作间加入非固定延时，避免机械等间隔行为
3. **异常熔断**：`try/catch` 捕获 + `state.stats.failed` 失败计数；识别"频控/限制弹窗"（`chat-block-dialog / limit-dialog` 选择器）并中止/降速
4. **行为模拟**：`scrollIntoView` 滚动定位、FocusEvent/KeyboardEvent 拟人化事件序列

**合规口径**：站点描述为"仅作技术学习与个人求职辅助用途，内置限速与熔断以控制运行频率"，客观呈现工程实现，不夸大场景、不提供规避检测的方法论。面试中被问到时，建议同样按"风控设计"角度展开。

### 3. 中文语音情感识别系统（P3 · 毕业设计）

**评审范围**：项目 README、`backend/`（models: acoustic/asr/llm_analyzer/fusion）、`frontend/`（Vue3）

**技术架构**：FastAPI 分层后端（api/models/services/db）+ Vue3 + Element Plus + ECharts 前端。

**核心实现（源码实证）**：
- 声学通道：HuBERT（facebook/hubert-base-ls960）微调情感分类器，CASIA 数据集（4 发音人 × 6 情感 × 50 句 = 1200 条），准确率 **79.6%**
- 语义通道：faster-whisper 语音转写 + DeepSeek V3 语义情感分析
- 多模态融合：声学 + 语义**动态加权 Late Fusion**
- 交互闭环：Web Speech API 语音播报（`voice_assistant.py` 生成播报文本）、录音/波形/频谱组件、历史记录
- 一键启动：双击 bat 即可运行前后端

### 4. 网页记账本系统（P4 · 已上线）

**评审范围**：`D:\workbuddy`（`打工人小账本.html` 等，v1→v3.1 共 20+ 个版本文件，含修复说明与跨平台技术方案文档）

**核心实现**：
- 单文件 HTML，零框架依赖，localStorage 持久化，已部署 GitHub Pages
- 业务建模：日常/大额/固定三类支出分类，`availableBudget` 可用预算计算规则（固定支出不计入当期占用）
- 版本管理：`v1 → v3.1` 增量迭代，`// 优化-xxx` 注释规范标注每次改动，保留备份文件可回退

---

## 三、能力信息提取说明（简历 → 站点映射）

| 简历能力项 | 来源核验 | 站内呈现位置 |
|-----------|---------|-------------|
| 低代码平台搭建 | 致远 V8 实习实战（实习经历素材文档） | 实习经历 STAR + 技能栈 |
| 工作流/审批流编排 | V8 平台流程配置与审批节点调试 | 实习经历 + 技能栈 |
| RAG 全链路开发 | 简历能力项（本地无独立仓库源码） | 技能栈（未虚构为项目） |
| 智能体工作流搭建 | 简历能力项 + AI 辅助开发实践 | 技能栈 |
| AI 辅助开发（Codex/Claude） | 本机实际使用记录 | 技能栈独立分类 |

## 四、待补充清单（不影响上线）

1. **真实截图**：BOSS 看板、语音系统界面、记账本界面——替换 `assets/boss-dash.svg / speech-ui.svg / ledger-ui.svg` 为同名 `.png` 并改 index.html 中 `src`（每个图下方有注释标注）
2. **校园经历数字**：活动场次、覆盖社团数、参与人数——index.html「校园经历」处有 `TODO` 注释标记
3. **GitHub 仓库名核对**：确认 `WuxingAi66` 与实际账号一致（`WuxingAi666`？），不一致请全局替换
4. **果蔬项目 GitHub 链接**：如后续开源，替换项目卡片中的 GitHub 链接
